# Where the Next.js server forwards /api/* requests. Server-side only - the
# browser always talks to /api on the site's own origin.
# In Docker compose this is the API service name.
API_URL=http://localhost:4000
